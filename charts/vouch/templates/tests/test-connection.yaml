apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vouch.fullname" . }}-test-connection"
  labels:
    {{- include "vouch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vouch.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
